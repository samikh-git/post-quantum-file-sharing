SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# Optional: only for legacy HS256 (symmetric). ES256 signing keys use JWKS from SUPABASE_URL — leave unset.
SUPABASE_JWT_SECRET=
FRONTEND_URL=http://localhost:5173
